[package]
name = "arbiter-macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true
doctest = false

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "3.0", features = ["derive", "fold", "full", "visit-mut"] }

[dev-dependencies]
arbiter-crypto = { path = "../arbiter-crypto" }

[lints]
workspace = true
